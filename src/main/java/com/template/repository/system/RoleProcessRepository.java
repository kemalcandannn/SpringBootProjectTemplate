package com.template.repository.system;

import org.springframework.data.jpa.repository.JpaRepository;

import com.template.entity.system.RoleProcess;

public interface RoleProcessRepository extends JpaRepository<RoleProcess, Long> {
}
